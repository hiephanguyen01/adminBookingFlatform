import React from "react";
import "./Header.scss";

const Header = () => {
  return (
    <div className="Header">
      <h2>This is header for later (not need now)</h2>
    </div>
  );
};

export default Header;
